#include <stdio.h>

/**
 * area - calculates the area of a square
 * @num1: the length of the square
 * @num2: the width of the square
 * Return: the area
 */
int area(int num1, int num2)
{
	return (num1 * num2);
}
